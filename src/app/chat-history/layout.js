export const metadata = {
  title: 'Chat History | Reflecto',
  description: 'View and manage your chat conversations with Luna AI',
};

export default function ChatHistoryLayout({ children }) {
  return (
    <div className="h-full">
      {children}
    </div>
  );
}
